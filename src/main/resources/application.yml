spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/search_engine}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          "[batch_size]": 50
        "[format_sql]": false
    open-in-view: false
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2s

server:
  port: 8080

search:
  embedding:
    enabled: ${EMBEDDING_ENABLED:false}
    provider: mistral
    api-key: ${MISTRAL_API_KEY:}
    model: mistral-embed
    dimensions: 1024
    timeout-millis: 5000
    batch-size: 10
  llm:
    enabled: ${LLM_ENABLED:false}
    base-url: https://api.mistral.ai/v1
    api-key: ${MISTRAL_API_KEY:}
    model: ${LLM_MODEL:mistral-small-latest}
    max-tokens: 512
    timeout-seconds: 10
  jina:
    search-api-key: ${JINA_API_KEY:}
    search-url: https://s.jina.ai
    timeout-millis: 8000
  rate-limit:
    max-concurrent: 100
    max-requests-per-minute: 30
    bot-check-enabled: true
  cache:
    search-ttl-minutes: 10
  enrichment:
    enabled: true
    top-n: 5
    max-concurrent-fetches: 20
    per-host-limit: 5
    fetch-timeout-millis: 4000
  providers:
    stackoverflow:
      base-url: https://api.stackexchange.com
      api-key: ${STACKEXCHANGE_API_KEY:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
